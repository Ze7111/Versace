func start() {
    out < "Testing from a module" < newl < green
    in num1 < "Enter a number: " < green < endl < str
    out < f"sending {num1} to the main file now!" < green < newl 
    return num1
}
